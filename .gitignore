# datos de corridas (no se versionan)
scripts/data/
**/data/
*.pyc
__pycache__/

# credenciales (nunca subir)
config.json
.env
.env.*
# la plantilla SÍ se versiona (no trae llaves)
!.env.example

# macOS
.DS_Store

# notas locales de trabajo (nunca se suben)
CONTEXTO-LOCAL.md
*.local.md
