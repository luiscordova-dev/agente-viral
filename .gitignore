# datos de corridas (no se versionan)
scripts/data/
**/data/
*.pyc
__pycache__/

# credenciales (nunca subir)
config.json
.env.*
# El .env SÍ se versiona, vacío y con instrucciones: así quien clona el repo
# ya lo tiene ahí para pegar sus llaves, sin buscar nada. El agente lo limpia
# solo en cuanto las guarda en ~/.agente-viral/.
!.env

# macOS
.DS_Store

# notas locales de trabajo (nunca se suben)
CONTEXTO-LOCAL.md
*.local.md
